/**
 * Landing page configuration.
 *
 * CTA URL is resolved from centralized site config
 * (NEXT_PUBLIC_LP_CTA_URL → NEXT_PUBLIC_CTA_URL → default).
 */

export { siteConfig } from './site-config'

